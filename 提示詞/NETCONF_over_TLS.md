```
提示詞 請生成一張 16:9、4K 視覺品質、高細節、適合技術簡報投影片使用的繁體中文資訊圖。   主題： O-RU Onboarding：從出廠 IDevID 到正式營運 LDevID，再到 NETCONF over TLS 連線的完整流程。   整體風格： 企業級技術簡報風格、乾淨、專業、白底、藍綠紫主色系、細線框、icon-based infographic、清楚的流程箭頭、適合投影片投影。畫面要清晰、留白充足、文字盡量大而可讀，不要塞太多小字。整張圖像高解析、邊線銳利、像向量圖。   畫面比例與解析度： 16:9 landscape, presentation slide, ultra high resolution, 4K visual quality.   版面配置： 使用橫向 swimlane 流程圖，分成四個主要角色欄位： 1. O-RU 2. Access Network / 802.1X Authenticator / DHCP / DNS 3. Operator PKI / CA-RA 4. O-RU Controller / O-DU / SMO   流程分成三大階段，使用頂部彩色階段條： A. 出廠憑證階段 IDevID B. 正式營運憑證階段 LDevID C. NETCONF over TLS 連線階段   請包含以下流程步驟，使用 0 到 8 的大圓形編號，橫向繪製：   0. 出廠狀態 - O-RU 內建 IDevID / 出廠憑證 - 預載 Vendor Root CA / Trust Anchor - 尚未取得 Operator LDevID   1. 第一次上電 / Start-up - Link up - 載入出廠設定 - 啟動 802.1X supplicant，如果部署有啟用 802.1X 2. 第一次 802.1X / EAP-TLS - 802.1X 使用憑證：IDevID-8021X certificate - 若尚未有有效 operator-signed certificate，使用 manufacturer-installed / vendor-signed certificate - Switch / Authenticator / RADIUS Server 驗證 O-RU - 成功後狀態：Limited Authorized   3. 取得網路與服務發現 - DHCP 取得 IP - DNS 解析 - 發現 O-RU Controller IP/FQDN - 發現 CA/RA Server IP/FQDN - 取得 Call Home protocol，例如 NETCONF over TLS   4. 申請 LDevID / CMPv2 Certificate Enrolment - O-RU 使用出廠 IDevID 證明身份 - 產生 CSR - CSR 包含 manufacturer、model、serial number - CA/RA 驗證 Vendor Root CA、serial number、inventory policy - CA/RA 簽發 operator-signed certificate - 回傳 LDevID、certificate chain、operator trust anchor   5. 安裝 LDevID 並重新初始化 - O-RU 安裝 operator-issued certificate - 憑證與 trust anchor 存入 reset-persistent memory - O-RU re-initializes start-up procedure - 標註：可能是 full reboot，也可能只是 re-init，依供應商實作   6. 第二次 802.1X / EAP-TLS - 802.1X 使用憑證：LDevID-8021X certificate - 若 O-RU 已有有效 operator-signed certificate，EAP-TLS 應使用 operator-signed certificate - Switch / Authenticator / RADIUS Server 再次驗證 - 成功後狀態：Authorized   7. NETCONF over TLS Call Home - O-RU 主動 TCP connect 到 O-RU Controller 它是TCP Client - 預設 NETCONF/TLS Call Home port：4335 - O-RU 在 NETCONF/TLS 應用角色中是 NETCONF Server - O-RU Controller 是 NETCONF Client - 建立 TLS 1.2 / TLS 1.3 mutual authentication - 補充: O-RU在NETCONF/TLS應用中是TLS Server但O-RU在802.1X EAP-TLS中是TLS Client   8. NETCONF Session 建立 - 使用 LDevID-M-Plane certificate - Controller 驗證 O-RU certificate chain - O-RU 驗證 Controller certificate chain - NETCONF <hello> capability exchange - certificate-to-NETCONF username mapping - NACM / RBAC 權限 - 後續進行 O-RU provisioning、S-Plane、C/U-Plane、FM、PM 設定 特別強調憑證使用： 在圖中下方做一個「憑證使用總結」區塊，分成四張卡片：   1. IDevID-8021X 用途：第一次 802.1X / EAP-TLS 來源：出廠憑證 安全重點：僅用於初始網路接入   2. IDevID-M-Plane Bootstrap 用途：CMPv2 enrolment / 申請 LDevID 來源：出廠憑證 安全重點：證明設備身份，不作正式營運管理憑證   3. LDevID-8021X 用途：正式營運後的 802.1X / EAP-TLS 來源：Operator PKI 安全重點：網路接入身份   4. LDevID-M-Plane 用途：NETCONF over TLS mutual authentication 來源：Operator PKI 安全重點：M-Plane 管理身份   請在圖中用醒目註解標示： 「重要觀念：O-RAN M-Plane 規格要求 802.1X 在取得有效 operator-signed certificate 後使用該憑證進行 EAP-TLS；但規格不強制 802.1X 憑證與 M-Plane 憑證一定要是同一張或不同張。 圖例設計： - 藍色實線：標準概念 / 規格流程   - 橘色虛線框：供應商或營運商實作決定 - 綠色：營運商 LDevID / Operator PKI - 紫色：NETCONF over TLS / M-Plane - 鎖頭 icon：憑證與 TLS - 伺服器 icon：CA/RA、Controller - 交換器 icon：802.1X Authenticator   右側放兩個小框： 框一：哪些是標準概念？ - CMPv2 certificate enrolment - 安裝 operator certificate 後重新初始化 start-up - 有效 operator certificate 存在時，802.1X EAP-TLS 使用 operator certificate - NETCONF over TLS 使用 X.509 mutual authentication - NETCONF/TLS Call Home 預設 port 4335 - O-RU在憑證到期前30天,10天,2天，逾期時，通知O-RU Controller - O-RU不得自主發起金鑰或是憑證更新，必須完全由 O-RU Controller負責主動觸發，來啟動 CMPv2 的憑證更新程序   框二：哪些由供應商 / 營運商實作決定？ - 出廠是否預設啟用 802.1X - DHCP / DNS / VLAN / CA-RA discovery 細節 - full reboot 或 re-init - 802.1X 與 M-Plane 是否使用不同憑證、不同金鑰、不同 EKU - unauthenticated connectivity 策略 - SAN mapping、NACM、RBAC、帳號政策   視覺要求： 請避免過多密集小字。主要文字必須清楚可讀。使用簡潔 icon、箭頭、卡片式區塊。整體像高品質資安架構投影片，不要卡通風，不要手繪風，不要 3D 過度炫光。保持專業、清楚、可投影。   不要生成照片風格，不要生成真實設備照片。請生成乾淨的向量資訊圖風格。
```
